<?xml version="1.0" encoding="utf-8"?>
<!--
   Sentinelle's principle is that nothing leaves the device — Android's
   Auto Backup (to the user's own Google account) technically counts,
   even though it's not Sentinelle's own infrastructure. Excluded by
   whole domain rather than by individual table/file name, so a table
   added later doesn't silently start getting backed up until someone
   remembers to add a rule for it.

   database: Room (call/SMS history, blocked events, labels, pattern lists)
   file:     DataStore preferences (device UUID, MDM org API key, settings)
   sharedpref: legacy key-value prefs, none currently used, excluded anyway
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
