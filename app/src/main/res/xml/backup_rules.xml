<?xml version="1.0" encoding="utf-8"?>
<!--
   Legacy counterpart to data_extraction_rules.xml, used on API 29-30
   (minSdk 29) where android:dataExtractionRules doesn't apply yet. Same
   reasoning, same domains — keep the two in sync.
-->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
    <exclude domain="sharedpref" path="." />
</full-backup-content>
